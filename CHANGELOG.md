# 变更记录

## 0.54

### 🔧 玩家可见修复
- **联机 Shift 取出物品消失**：服务端直接改写玩家背包会被客户端后续同步覆盖掉（客户端对自己的背包才是权威），
  改为回发包让客户端自己写进背包；背包放不下时只取走放得下的部分，剩下的留在机器里。
- **联机 Shift 合成的目的地**：合成请求现在带上“进背包 / 进鼠标”，Shift 合成先由服务端确认产物能整个放进背包，
  放不下就这一次不合成（材料一点都不扣），与最初的行为一致。
- **联机交互音效**：客户端不再执行本地搬运后音效一并消失了（服务端播音在专用服务器上没人听得到）。
  音效改由客户端在收到服务端结算结果时播放，并按动作区分（取出 / 合成 = Grab，放入 / 交换 / 拒绝 = MenuTick）。
- **协议版本 1 → 2**：回发包新增“交付目的地”与“音效”字段，新旧版本不再混用（版本不符会被丢弃并告警）。
- **快速左右键狂点槽位刷物品（已定位到具体路径）**：拖拽会话的回滚会把**旧快照**直接写到鼠标上。
  快速左右键交替时会出现“先把鼠标物品放进槽位、再回滚上一次拖拽”的顺序，于是槽位里的物品留着、鼠标上又凭空多出同样一叠；
  鼠标上如果是别的物品则会被直接覆盖抹掉。现在回滚按守恒结算：只把“这次拖拽真正放进槽位的数量”还给鼠标，
  没有改动过任何槽位就完全不碰鼠标，鼠标装不下时放弃回滚并保持现状。
  另外，拖拽过程中若鼠标上的物品已被其它操作换成别的种类，该次分堆会被跳过（否则会把鼠标上的另一种物品变成快照类型）；
  只是跳过这一次分堆，**会话本身保留**，按住左键拖拽时按中键 / 右键仍可正常回滚。
- **拖拽回滚**：没有进行中的拖拽时，回滚不再拿上一次的旧快照去覆盖鼠标上的物品（此前会把鼠标物品改成旧数量，
  多则刷物品、少则凭空消失）；拖拽结束时快照一并作废。回滚本身保留原有手感：
  左键拖拽中按中键 / 右键、右键拖拽中按中键 / 左键均可取消本次拖拽并还原到操作前。
- **配方自动填装的材料分配**：材料池不足时不再用 `new Item(type, n)` 凭空补足、也不再返回比实际取到的更多的数量
  （两条都会直接复制材料）。
- **快速左右键狂点槽位刷物品**：
  * **交互节流**：同一个槽位两次交互之间至少间隔 4 tick（仿照原版槽位点击延迟）。快速连点时同一次操作会被派发多次
    （单击 + 双击、按下 + 抬起），现在只结算第一次。
  * **守恒守卫**：每次“鼠标 ↔ 槽位”移动都会核对 `槽位 + 鼠标` 的物品总数；一旦变多就**回滚这次交互**并写一条
    `[SlotGuard]` 警告（宁可这次点击不生效，也不凭空多出物品）。这条兜底覆盖任何我们还没找到的复制路径。
  * **拖拽状态卫生**：开始新拖拽前先清掉上一轮的残留（快照 / 原始堆叠），退出界面时也清一次；
    同一 tick 内只结算一次拖拽（绘制可能一帧被调用多次）。
  * **写回时机**：槽位被“双击收集 / 配方自动填装 / 合成消耗材料”直接改动后**立即**写回物块实体（此前要等下一帧的变更检测），
    并且同一个工作台再次打开时**复用**已有界面、关闭前也写回一次——此前界面重建会把实体里的旧内容搬回槽位，
    而玩家手上已经拿到了那份物品（这条已用无头反证复现：关闭写回时重开界面物品 13 → 23）。
- **合成产物兜底**：材料已消耗但鼠标 / 背包都没能取走产物时（例如鼠标上放着别的物品且背包已满），
  余量会立刻转入背包、背包也满则掉落在脚下——此前余量留在合成槽里，下一帧被配方结果重算覆盖，材料没了产物也没了。
- **联机压缩机取物 / 放物会复制物品（已定位并修复）**：客户端过去按自己那份**已过期的镜像**算出绝对数量、
  服务端照抄，而输入槽每 3 tick 就被服务端消耗一次：每取一次就多出 1 个（实测 13 次取物共 +8，
  玩家持有总量 10717 → 11393 = +676）。现在联机下客户端只发**意图**（放入 / 放入一个 / 全部取出 /
  取出一半 / 送背包 / 丢弃 / 交换），数量一律由服务端按真实数据结算，再回发包告诉客户端鼠标该扣多少、该收多少；
  同一时刻只允许一笔交易在飞（回包即解除，不是点击延迟）。
- **联机工作台多人同时合成**：材料在每个客户端各有一份镜像，各自判定配方、各自扣材料会让同一份材料合出多份产物。
  现在联机下合成槽只发“合成请求”（Shift 为连合），由**服务端按自己槽位里的材料**重新匹配配方、扣材料、
  把产物回发到请求者鼠标，并把整台内容物广播给所有客户端。两人同时合成时按顺序结算，
  第二个人要么用剩下的材料再合一份，要么直接失败（不会重复扣同一份材料）。
- **收集器输出槽**：改用同一套“服务端结算数量”的协议（产出由服务端持续生成，客户端镜像必然过期）。
  多个玩家同时点同一个输出槽时，服务端按顺序结算，每人只拿到“轮到自己时还剩多少”，不会两人各取一份。
  旧的“提交绝对数量”发包函数已标记废弃。
- **双击收集（鼠标装满时）**：部分收集也会立即写回物块实体，不再等到下一帧或界面关闭。
- **回退 0.53 的交互节流**：“两次交互间隔 4 tick”与“双击后 12 帧内忽略单击”两处延迟已移除——
  原版物品栏 / 箱子没有这种处理，而刷物品的真正原因（拖拽回滚）已在 0.54 定位并修复。
  仅保留“取消拖拽后的 12 tick 冷却”，否则取消的那一下点击会立刻把物品又搬走。

## 0.53

### 🔧 玩家可见修复
- **范围挖掘的掉落**：模组方块改走 `ModTile.GetItemDrops`（工作台、收集器、压缩机里存的物品都算在内），
  家具与箱子本体也会被收进物质团；软物块恢复“只破坏、不产出掉落”；家具不再掉两份。
- **矿石倍率**：×4~40 曾静默失效（破坏后方块类型已被清零），现在在破坏前记录类型。
- **墙体**：单人下非环境墙的掉落进物质团；多人下抹墙改由服务端权威执行（此前客户端抹了会被服务端同步回来）。
- **罐子等“破坏时现算随机奖励”的方块**：奖励不再漏到世界上（直接破坏、支撑被拆的延迟级联、多人挥动割除三条路径都覆盖）。
- **物质团里的工作台数据**：不再按类型重建物品（会丢掉内容物 / 形态 / maxStack）；内容不同的同种工作台不再被并成一件；
  从物质团倒出后捡起也不会被背包里的空工作台“吸”掉内容物。
- **紧挨箱子的方块**：修复“挖掉了却什么都不掉”（原版 `Chest.FindChestByGuessing` 会把附近任意格子猜成箱子）。
- **工作台**：放置后其它端打开为空、多人客户端凭空产出“没有内容物的工作台”两个问题已修。
- **压缩机 / 收集器（开服联机的主机端）**：槽位改动只在单人模式写回物块实体，主机端（`netMode == Server`）会丢失改动；
  现在与服务端写回逻辑对齐。
- **其它**：配方列表高亮不取消、Shift+点击合成槽被拖拽吞掉、普通模式挖带物品的箱子、
  NatureRuin 越界 / 蔓藤 / 草皮、HeavenArrow 联机发包过多。

### 🔒 联机安全
- **方块数据写入加校验**：工作台内容物上传 / 回传、工作台槽位、压缩机与收集器槽位，服务端现在要求
  “发包玩家合法且目标方块在其可达范围内”，否则忽略（此前任何客户端都能改写别人的机器内容，可用于复制物品）。
- **宇宙球体状态**：服务端一律以发包玩家为准，忽略包内提供的玩家下标。
- **包体上界**：宇宙球体状态快照的条目数加了上界校验，伪造包不再能让读取越界。
- **协议版本**：本模组所有包都带上 `ProtocolVersion`，不一致的包直接丢弃并告警（避免新旧版本静默错位）；
  消息枚举写死数值，调整顺序不再改变协议。

### 🎮 新增与改进
- **网络层统一**：模组自有消息收拢到 `AvaritiaNet`，成对字段读写抽成辅助方法；范围挖掘的批量破坏按 400 格分片，
  抹墙按包围盒整片交给服务端。
- **物质团内容按物品实例保存**：存档 / 联机 / 倒出都以实例为单位，不再依赖单例字段。
- **配方匹配缓存**：以槽位内容快照为键，避免每帧全量遍历。
- **形态系统**：形态绑定到物品实例并随克隆复制，多人下同步（实现见 EternalLib 的 `FrameItem` / `FramePlayer`）。

### ⚠️ 破坏性变更
- 网络消息枚举顺序调整（客户端与服务端必须使用同一版本）。
- `WorldBreaker` / `PlanetEater` 改为继承 `AoeToolItem`；删除 `AvaritiaModeItem` 与 `AvaritiaDropCollector`。

### 🛠️ 内部重构
- 方块破坏、掉落生成与预测、多格归一化、箱子、抹墙、范围挖掘与多人结算全部下沉到 **EternalLib**；
  本模组只保留物质团这一步（`AvaritiaBreakHelper.SpawnAsClusters` 与合并规则）。
- 范围挖掘的接入方式：工具实现库的 `IAoeMiningTool`（额外掉落表 + 掉落交付），不再往库里写全局静态钩子。
- **静态状态复位移到一处**（`AvaritiaStateResetSystem`）：放置端暂存的工作台内容物改为共用持有者
  （`CraftingTablePending`）并在换世界 / 卸载时清空，配方注册表与缓存、默认 `maxStack` 缓存也在卸载时清空
  ——此前热重载或换世界后会残留上一份数据。
- 压缩机服务端广播由“每 tick 整台发包”改为“状态变化时发包 + 每 30 tick 兜底”，与收集器一致。
- 追踪型弹幕找目标不再对 `Main.npc` 开 LINQ（改用库的数组版最近实体查询）。
- 诊断日志收敛：只在异常时写日志；库统一走 `EternalLog`，日志与异常消息使用英文。
